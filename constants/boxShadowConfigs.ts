import { Shadow, Box } from "@/components/types";

export const defaultShadowSettings: Shadow = {
  horizontalOffset: 0,
  verticalOffset: 0,
  blur: 15,
  spread: 4,
  shadowColor: "#0000001a",
  inset: false,
};

export const defaultBoxSettings: Box = {
  bgColor: "#ffffff",
  borderRadius: 6,
  boxColor: "#3F83F8",
};
